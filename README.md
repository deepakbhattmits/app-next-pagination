<!-- @format -->

# Example

To run this example:

- `npm install` or `yarn`
- `npm run dev` or `yarn dev`

# In this example we will learn how we can use folowing hooks from `react-query`

### 1. usePaginatedQuery

### 2. useInfiniteQuery
